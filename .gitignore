# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# Outreach data — contains named contact details for real people (TPOs).
# This repo is pushed to a public GitHub remote; committing personal data
# here would publish it. Keep the trackers local.
/outreach/

# Claude Code / Cowork local tool settings — not part of the app.
/.claude/

# Scratch folder for files that couldn't be deleted via the remote device
# bridge (it can move files but not unlink them) — pending manual deletion.
/_to_delete/
